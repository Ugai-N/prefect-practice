# Prefect Server Setup
pip install -U prefect

prefect server start

python start.py

# Prefect UI
After running the above commands, open your web browser and navigate to http://localhost:4200 to access the Prefect Server UI.